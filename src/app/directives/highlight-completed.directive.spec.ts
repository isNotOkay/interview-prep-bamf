import { HighlightCompletedDirective } from './highlight-completed.directive';

describe('HighlightCompletedDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightCompletedDirective();
    expect(directive).toBeTruthy();
  });
});
